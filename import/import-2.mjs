// Named Exports

export function greet(name) {
  return `Hello ${name}`;
}

export function goodbye(name) {
  return `Bussi 😘 baba ${name}`;
}

// Default Export

export default function lunchBreak(name) {
  return `${name} is HUNGREEEY!`;
}

// Another way of doing it:
// export { greet, goodbye };
